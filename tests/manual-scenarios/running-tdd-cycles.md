---
purpose: Sample prompts that should retrieve running-tdd-cycles
---

# running-tdd-cycles — manual scenarios

1. "Add a function to validate ISO8601 timestamps using TDD."
2. "Write a failing test for the rate limiter."
3. "I want to add this feature one requirement at a time — start with the test."
4. "We just got the GREEN, now refactor with the test as a safety net."
5. "I think we drifted into test-after — get us back on the rails."
